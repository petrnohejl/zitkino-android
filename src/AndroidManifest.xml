<?xml version="1.0" encoding="utf-8"?>

<!--
DO BEFORE UPDATE TO GOOGLE PLAY:
-set new version in manifest
-set map API key
-set GCM sender ID
-set Facebook app ID
-set Android key hash on developers.facebook.com
-set Localytics/Flurry API key
-add Localytics/Flurry events for new features
-edit app changelog
-edit text in about and on Google Play page
-edit text in dialog for new version
-remove logs
-monkey test
-post message about update to social networks


RELEASE INFO:
-code:1 - name:1.0 - date:23.08.2012


TODO:
-dialog predelat na dialog fragment a zajistit zobrazeni i pri otoceni displeje
-pamatovat si pozici v listu pri otoceni
-Flurry


TESTS:
-test app on all Android versions (handset, tablet)
-monkey test (fast clicking, changing orientation, offline mode)
-test migration from older version to newer
-test offline mode
-test slow internet connection
-test empty views
-test reboot (boot receivers, alarms, services)
-test Localytics/Flurry tracking
-->


<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="cz.petrnohejl.zitkino"
	android:versionCode="1"
	android:versionName="1.0"
	android:installLocation="auto">
	
	<uses-sdk android:minSdkVersion="7" android:targetSdkVersion="16" />
	
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application
		android:name=".ZitkinoApplication" 
		android:icon="@drawable/ic_launcher"
		android:logo="@drawable/ab_logo"
		android:label="@string/app_name"
		android:theme="@style/Theme.Zitkino"
		android:hardwareAccelerated="true">

		<activity
			android:name=".activity.ListingActivity"
			android:label="@string/app_name"
			android:launchMode="standard"
			android:uiOptions="none">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

	</application>
</manifest>
